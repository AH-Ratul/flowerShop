<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Processing</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f9f9f9;
            font-family: Arial, sans-serif;
        }
        .loader {
            border: 8px solid #f3f3f3;
            border-top: 8px solid #E55473;
            border-radius: 50%;
            width: 50px;
            height: 50px;
            animation: spin 1s linear infinite;
        }
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        .message {
            margin-left: 20px;
            font-size: 18px;
        }
    </style>
</head>
<body>
    <div class="loader"></div>
    <div class="message">Processing your order, please wait...</div>

    <script>
        setTimeout(() => {
            window.location.href = "/app/invoice/<%= order_id %>";
        }, 3000); // Redirect after 3 seconds
    </script>
</body>
</html>
